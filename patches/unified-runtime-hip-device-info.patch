diff --git a/source/adapters/hip/device.cpp b/source/adapters/hip/device.cpp
index f07a31c..a5a02d3 100644
--- a/source/adapters/hip/device.cpp
+++ b/source/adapters/hip/device.cpp
@@ -1067,6 +1067,20 @@ UR_APIEXPORT ur_result_t UR_APICALL urDeviceGetInfo(ur_device_handle_t hDevice,
     return ReturnValue(false);
   case UR_DEVICE_INFO_DYNAMIC_LINK_SUPPORT_EXP:
     return ReturnValue(false);
+  case UR_DEVICE_INFO_ASYNC_USM_ALLOCATIONS_SUPPORT_EXP:
+    return ReturnValue(false);
+  case UR_DEVICE_INFO_LUID:
+    return UR_RESULT_ERROR_UNSUPPORTED_FEATURE;
+  case UR_DEVICE_INFO_NODE_MASK:
+    return UR_RESULT_ERROR_UNSUPPORTED_FEATURE;
+  case UR_DEVICE_INFO_CLOCK_SUB_GROUP_SUPPORT_EXP:
+  case UR_DEVICE_INFO_CLOCK_WORK_GROUP_SUPPORT_EXP:
+  case UR_DEVICE_INFO_CLOCK_DEVICE_SUPPORT_EXP:
+    return ReturnValue(false);
+  case UR_DEVICE_INFO_IS_INTEGRATED_GPU:
+    // HIP does not currently expose this through Unified Runtime. Returning a
+    // value keeps optional SYCL device-aspect probes from failing enumeration.
+    return ReturnValue(false);
   case UR_DEVICE_INFO_KERNEL_LAUNCH_CAPABILITIES:
     return ReturnValue(0);
   case UR_DEVICE_INFO_MEMORY_EXPORT_EXPORTABLE_DEVICE_MEM_EXP:
